#include "main.h"
/**
 * _isdigit - see number between a and b
 *
 *@a: int a es un numero cualquiera
 *@b: int b es un numero cualquiera
 *
 *  Return: always 0
 */
int mul(int a, int b)
{

int resultado;

resultado = a * b;
if (resultado == 4)
{
resultado = 4;	
}
return (0);
}
