#include "main.h"
/**
 * _isdigit - see if is c is between 0 and 9
 *
 *@c: int c es numero cualquiera
 *
 *  Return:  1 if c is a digit otherwise 0
 */
int _isdigit(int c)
{
if (c >= 0 && c <= 9)
{
return (1);
}
return (0);
}
