#include "main.h"
/**
* *string_toupper - copy string pointed
*@n:a char
*Return: void
*
*/
char *string_toupper(char *n)
{
/*
int i = 0; 
int countn = 0;

while (*n != '\0')
{
countn++;
n++;
}

for ( i = 0 ; i < countn ; i++)
{
if(n[i] >=  'a'  && n[i] <= 'z' )
{
n[i] =(n[i] - 32);
}
}
*/
return (n);
}
