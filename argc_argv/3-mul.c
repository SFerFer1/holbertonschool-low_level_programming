#include "main.h"
/**
*main - return number
*@argv: a string of string
*@argc: a int
*Return: int
*
*/
int main(int argc, char *argv[], int a ,int b)
{
int resultado;
char error["Error"];
int i;

(void)argv
if ( argc > 3)
{
for (i = 0; error[i] != '\0'; i++)
{
_putchar(error[i]);
}
}
else
{
resultado = a *b;
printf("%d\n", resultado);

}
return (0)
}
